[llamada make OK] Como esperaba, solo habia que instalar Make for Windows y añadirlo como variable de entorno, el que venía en el paquete MinGW no funcionaba.

Configurar mi CMake para que tome correctamente el generador minGW por defecto, actualmente esta NMake y no he conseguido cambiarlo mediante 
el comando cmake -G "MinGW Makefiles", ni se encuentran las variabales de entorno, por lo que requiere generar el fichero CMakeCache desde CMake-gui
para indicarle el compilador a utilizar y una vez generado, si que permite compilar desde terminal.